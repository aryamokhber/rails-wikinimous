<h1><%= @article.title %><%= link_to 'Update', edit_article_path(@article) %><%= link_to 'Delete', article_path(@article), method: :delete, data: { confirm: 'Are you sure?'} %></h1>
<p><%= @article.content %></p>
